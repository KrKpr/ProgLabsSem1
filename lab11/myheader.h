#ifndef MYHEADER_H
#define MYHEADER_H

struct data {
    float largerRadius;
    float smallerRadius;
};

extern float SquareFunc(struct data ring);

extern float PerimeterFunc(struct data ring);

#endif
